import { Navbar } from 'react-bootstrap';
import Header from './Header';

const HeaderMain = () => {
    return (
        <div>
            <Navbar />
            <Header />
        </div>
    );
};

export default HeaderMain;