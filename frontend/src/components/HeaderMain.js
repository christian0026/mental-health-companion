import { Navbar } from 'react-bootstrap';
import Header from './Header';

const HeaderMain = () => {
    return (
        <div class='h_main'>
            <Navbar />
            <Header />
        </div>
    );
};

export default HeaderMain;